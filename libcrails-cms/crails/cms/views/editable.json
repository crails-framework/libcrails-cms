#include "../../../models/editable.hpp"

const Crails::Cms::Editable& @model;

// END LINKING

json("title",         model.get_title().to_string());
json("slug",          model.get_slug());
json("body",          model.get_body().to_string());
json("description",   model.get_description().to_string());
json("thumbnail_url", model.get_thumbnail_url());
